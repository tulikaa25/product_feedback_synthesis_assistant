# Database URL connection string.
# Leave blank (or omit) to use the self-contained local SQLite file database (dev.db).
# For production (Render) or PostgreSQL testing, paste your Postgres link here.
# DATABASE_URL="postgresql://username:password@localhost:5432/feedback_db?schema=public"

# Google AI Studio Gemini API Key
GEMINI_API_KEY="your-gemini-api-key-here"

# Server configuration
PORT=5000
